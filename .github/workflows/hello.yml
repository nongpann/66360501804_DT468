name: Hello world
on: 
 push:
  branches: ["main"]
 workflow_dispatch: 
jobs:
 hello1:
  runs-on: ubuntu-latest
  steps:
   - name: say hello
     run: echo "hello"
   - name: say name
     run: echo "papran"
 bye:
   needs: hello1
   runs-on: ubuntu-latest
   steps:
   - name: say bye
     run: echo "bye"
